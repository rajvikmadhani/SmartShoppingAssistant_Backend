//We are not using this. This is for future extension if we want a daily update automatically
import runDailyFullProductScrape from './scheduler/cronDailyProductScraper.js';

await runDailyFullProductScrape(); // Runs once and exits
